{% extends "layout.html.twig" %}

{% block title %}
Billetterie
{% endblock %}

{% block header %}
    <h1 style="text-align: center">Billetterie du musée</h1>
{% endblock %}

{% block body %}
    <p>
        <span class="glyphicon glyphicon-bullhorn" style="font-size:20px; color:#33b5e5"></span> Pour toute commande supérieure à 10 billets, veuillez passer par le formulaire de contact.
    </p>
    <h3><span class="glyphicon glyphicon-info-sign" style="font-size:25px; color:#33b5e5"></span> Tarifs du musée pour l'année 2018</h3>
    <div class="well">
                <div class="table-responsive-sm">
                    <table class="table table-sm">
                        <thead>
                        <tr>
                            <th scope="col">Enfant</th>
                            <th scope="col">Normal</th>
                            <th scope="col">Sénior</th>
                            <th scope="col">Réduit</th>
                        </tr>
                        </thead>

                        <tbody>
                        <tr>
                            <td scope="row">8€</td>
                            <td>16€</td>
                            <td>12€</td>
                            <td>10€</td>
                        </tr>
                        <tr>
                            <td scope="row">De 4 ans à 12 ans <em><br/>L’entrée est gratuite pour les enfants de moins de 4 ans</em></td>
                            <td>De 12 à 60 ans</td>
                            <td>60 ans et plus</td>
                            <td>Un justificatif sera demandé à toute personne ayant réservé un billet à tarif réduit. <br/><em>(étudiant, employé du musée, d’un service du Ministère de la Culture, militaire…)</em></td>
                        </tr>
                        </tbody>
                    </table>
                </div>
    </div>

<h3><span class="glyphicon glyphicon-tasks" style="font-size:25px; color:#33b5e5"></span> Informations concernant la commande</h3>

<div class="well">
    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

    {{ form_errors(form) }}

    {{ form_row(form.visitDate) }}

    {{ form_row(form.halfDay) }}
    <div>
        {% for message in app.session.flashbag.get('erreur') %}
            <p>Message flash : {{ message }}</p>
        {% endfor %}
    </div>

    {{ form_row(form.mail) }}

    {{ form_row(form.nbTickets) }}
    <div class="row">
        <div class="pull-right">
    <input type="submit" class="btn btn-info" value="Valider la commande" />
        </div>
    </div>
    {{ form_end(form) }}
</div>

{% endblock %}